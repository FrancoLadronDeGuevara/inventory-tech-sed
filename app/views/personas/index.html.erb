<div class="border-b bg-white border-gray-300 pl-6 py-2 shadow-sm text-xl font-bold">
  Personas
 </div>
<div class="mt-4 ml-4">
 <%= link_to dashboard_path, class: "text-gray-600 hover:text-gray-900 flex items-center" do %>
  <svg class="w-4 h-4 mr-1 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
  </svg>
  <span>Volver</span>
<% end %>
</div>
<div class="mx-4 overflow-x-auto min-h-[400px]">
  <table class="w-full divide-y divide-gray-200 mx-auto mt-4">
    <thead class="bg-gray-50">
      <tr>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Nombre</th>
        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Apellido</th>
        <th class="px-6 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Acciones</th>
      </tr>
    </thead>
    <tbody class="bg-white divide-y divide-gray-200">
      <% @personas.each do |persona| %>
        <tr>
          <td class="px-6 py-4 whitespace-nowrap"><%= persona.nombre %></td>
          <td class="px-6 py-4 whitespace-nowrap"><%= persona.apellido %></td>
          <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
            <%= link_to "✏️", edit_persona_path(persona), class: "text-indigo-600 hover:text-indigo-900 mr-2" %>
            <%= link_to "🗑️", persona_path(persona), method: :delete, data: { confirm: "¿Seguro que deseas eliminar esta persona?" }, class: "text-red-600 hover:text-red-900" %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<div class="mt-auto">
  <%= render "shared/pagination", paginable: @personas %>
</div>
